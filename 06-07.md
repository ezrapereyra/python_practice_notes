# 06: If 

- `if <condition>:` is next in our lesson, the conditional statement.
- it only runs if the condition returns `True`

```python
birth_year = 1982
current_year = 2013
age = current_year - birth_year
if age > 65: # this returns False as 31 /> 65
    print("Unc Status") # therefore this is skipped
print("You are", age, "years old")
```
```
You are 31 years old
```

- notice how the "You are..." line was still printed, in python, statement blocks (lines of code to be run within the condition) is held by an indent.

---

>*Coding Exercise: IHOPython*
- Write a program that reads an integer from input using `pancakes = int(input())`. If `pancakes` is more than `3`, print out `Yum!` otherwise, print out `Still hungry!`.

Answer:
<details>
<summary>Click to view spoiler</summary>

``` python
pancakes = int(input())
if pancakes > 3:
    print("Yum!")
if pancakes <= 3:  # a better condition is `else` to avoid checking again, but we are learning only if statements atp in time
    print("Still hungry!")
```
</details>

---

>*Scramble Exercise: Unexpected Indent*
- Cause the following error:
```
IndentationError: unexpected indent
```
```python
if 1000 < 10:
   print("message 1")
  print("message 2")
if 2 > 1:
  print("message 3")
```

Answer:
<details>
<summary>Click to view spoiler</summary>

``` python
if 1000 < 10:
  print("message 2")
   print("message 1") # see how there is an additional indent here, the program does not know where this belongs
if 2 > 1:
  print("message 3")

```
</details>

---

>*Scramble Exercise: Unexpeced Unindent*
- Cause the following error:
```
IndentationError: unindent does not match any outer indentation level
```
```python
if 1000 < 10:
  print("message 2")
if 2 > 1:
  print("message 3")
   print("message 1")
```

Answer:
<details>
<summary>Click to view spoiler</summary>

``` python

if 1000 < 10:
  print("message 2")
if 2 > 1:
   print("message 1")
  print("message 3") # this is the opposite of the first error, it's not part of the if block, but is not on the same level of it either
```
</details>

---

>*Scramble Exercise: Expected Indent*
- Cause the following error:
```
IndentationError: expected an indented block
```
```python
if 1000 < 10:
   print("message 1")
  print("message 2")
if 2 > 1:
  print("message 3")
```

Answer:
<details>
<summary>Click to view spoiler</summary>

``` python
if 1000 < 10: # unlike in cpp where the if block defaults to referring tp the next line, python strictly requires you to tell what it needs to run, which needs to be indented
if 2 > 1:
   print("message 1")
  print("message 2")
  print("message 3")
```
</details>

---

>*Scramble Exercise: Proper Indentation*

- Don't cause any error
```python
if 1000 < 10:
   print("message 1")
  print("message 2")
if 2 > 1:
  print("message 3")
```

Answer:
<details>
<summary>Click to view spoiler</summary>

``` python
if 1000 < 10:
   print("message 1")
if 2 > 1:
  print("message 2")
  print("message 3")
```
</details>

---

## True, False and `bool`

 - in our conditions, we have been using a simple comparison `> or <`. Generally, any true/false value is a Boolean
 - in python, we use the variable type `bool`
 - there is only two bool values `True` and `False`

```python
print(2>1)
b = False
print(b)
print(type(b), type(2>1), type(True))
```
```
True
False
<class 'bool'> <class 'bool'> <class 'bool'>
```

---

## Boolean Comparisons

- operators `> < <= >=` compare the two numbers and return a bool
- other operations include `==` for equality and `!=` for inequality. these two however works for any non-numberic value aswell
- **Note**: remember that `=` and `==` are two different operators

---

>*Coding Exercise: What's Your Sign?*

 - Write a program that reads an integer from input and outputs `Positive`, `Negative`, or `Zero` according to the input.
 ```python
 x = int(input())
 # delete this comment and enter your code here
 ```

Answer:
<details>
<summary>Click to view spoiler</summary>

``` python
x = int(input())
if x < 0:
    print("Negative")
if x > 0:
    print("Positive")
if x == 0:
    print("Zero")
```
``` python
# here is a more efficient answer
x = int(input())
if x < 0:
    print("Negative")
elif x > 0:
    print("Positive")
else:
    print("Zero")
```
</details>

---

 - We have introduced the concept of `block` before. You can have ablock inside a block (naughty~~)

```python
 if password=='openSesame':
  print('User logged on.')
  if hour>17:               # notice the indent in the if here
    print('Good evening!')
  print('Enter a command:') # and here, its outside the second if
```

---

>*Multiple Choice Exercise: Nested ifs*

 - Which of the following outputs is not possible for the given program above? [im not writing allat]

Answer:
<details>
<summary>Click to view spoiler</summary>

```
one line: 'User logged on.'

why? 'enter a command:' is inside the first if statement but also outside the second, meaning regardless of the second if, it will run if the first if is true
```
</details>


---

>*Coding Exercise: Age checker*

 - Write a program that asks for integer input for age. If the age is 18 or older, print `You can vote`. If 0 to 17, `Too young to vote`. If less than 0, `You are a time traveller`.
 ```python
 age = int(input())
 # delete this comment and enter your code here
 ```

 Answer:
<details>
<summary>Click to view spoiler</summary>

``` python
age = int(input())
if age >= 18:
    print("You can vote")
if age >= 0 and age <= 17:
    print("Too young to vote")
if age < 0:
    print("You are a time traveller")
```
``` python
# here is a more efficient answer, we dont use this becaus elif is not yet discussed
age = int(input())
if age > 17:
    print("You can vote")
elif age >= 0:
    print("Too young to vote")
else:
    print("You are a time traveller")
```
</details>

---
---

# 06D: Design, Debugging, and Donuts

 - This mini lessons tackles how to make designing and debugging your programs easier:
  - build one at a time
  - solve examples by hand
  - plan your code before you start
  - write the code
  - test the examples you solved by hand
  - test additional examples, push the code's capabilities to test its limits

## The Problem: Donut Calculator

Tim Horton's, a chain of coffee stores in Canada, sells timbits which are tasty donut spheres  

You have to organize several parties in the next few weeks, and you'll be ordering a different number of timbits for each party. So, you would like to make a program to calculate the price of an arbitrary number of timbits, in order to plan your budget. At your local branch, these are the prices:

```
1                 $0.20
10 (small box)    $1.99
20 (medium box)   $3.39
40 (large box)    $6.19
```

---

>*Build one part at a time*

Eventually, you'd like to make a program where the input is the number P of people at the party and the output is the cost to feed that many people including tax. At the highest level, there are three components to this program:

    calculate, for P people, what is the number T of timbits you should buy?
    calculate the price to buy exactly T timbits
    calculate the tax and overall cost.
However, you can work on these three parts one at a time. In fact, we recommend that you build one part at a time, testing and fixing each part before you proceed to work on the next part, because the larger your program is, the harder it is to find and fix each bug. It's easier to develop and test the three parts separately, before combining them into a single program. For the rest of this lesson, we will focus on step 2: calculate the price to buy exactly T timbits.

---

>*Solve some examples by hand*

In order to be able to tell the computer to do something, you need to be able to do it yourself. Let's work through an example ourselves: what's the price to buy exactly `45` timbits? Looking at the list of sizes, we can buy `one large box (40)`, which will cost `$6.19`. That leaves `45 - 40 = 5` more timbits that we need to buy individually, which is an additional cost of `5 * $0.20 = $1.00`. The total cost will be `$6.19 + $1.00 = $7.19`.

---

>*Plan your code before you start writing it*

What if we wanted to buy 4 or 456 timbits instead of 45? In general, we should buy as many big boxes as possible, then take a medium box and/or a small box if needed, and individual timbits to get the right total.

We said that you should plan your code before you start writing it. You can:

    give a short step-by-step description of the algorithm in English instead of Python
    use a diagram or flowchart to show the steps
    focus on the main ideas instead of the details, for now.

*Auth note: i recommend pseudocode, which honestly? just use python, shit's in literal english man*

---

>*Write the code*

*Auth note: Man, just read the website, I'm so bored. I'll just give the code step by step. try to write your own code*

Step 1: Get initial values
<details>
<summary>Click to view spoiler</summary>

``` python
timbitsLeft = int(input()) 
totalCost = 0              
```
</details>

Step 2: Get as many large boxes as possible
<details>
<summary>Click to view spoiler</summary>

``` python
bigBoxes = timbitsLeft / 40
totalCost = totalCost + bigBoxes * 6.19    # update the total price
timbitsLeft = timbitsLeft - 40 * bigBoxes  # calculate timbits still needed          
```
</details>

---

>*Debug Jumpscare*

Let's run the program so far

```
the input is 45
bigBoxes equals 1.125
totalCost equals 6.96375
now timbitsLeft equals 0.0
```

Uh oh, I don't think TH allows us to buy 1.125 large boxes.

Where do you did it go wrong for you? Tell me about your past, how did it make you feel?

Debug quiz time: Where is the bug and whats the fix

Answer:
<details>
<summary>Click to view spoiler</summary>

``` python
bigBoxes = timbitsLeft // 40               # division returns a float! remember that we could either use floor division (//) or a typecast int(), im using floor div, the website used typecasting
totalCost = totalCost + bigBoxes * 6.19    # update the total price
timbitsLeft = timbitsLeft - 40 * bigBoxes  # calculate timbits still needed          
```
</details>

---


Step 3: Check if we can buy a medium or a small box
<details>
<summary>Click to view spoiler</summary>

``` python
# you can use the similar algorithm here, but lets not

# since atp, you will buy at most one medium and one small box, you can use a simple if statement

if timbitsLeft >= 20: # can we buy a medium?
  totalCost = totalCost + 3.39
  timbitsLeft = timbitsLeft - 20
if timbitsLeft >= 10: # can we buy a small?
  totalCost = totalCost + 1.99
  timbitsLeft = timbitsLeft - 20
```
</details>

Step 4: Buy the rest of the timbits
<details>
<summary>Click to view spoiler</summary>

``` python
totalCost = totalCost + timbitsLeft * 20
print(totalCost)
```
</details>

---

>*Debugging Jumpscare II*

ok, lets run the complete code now, input is `45`

```
106.19
```

great, we got scammed. I don't think i want to buy 45 timbits for over a 100 dollars

Debug quiz time: Find the bug and fix it

Answer:
<details>
<summary>Click to view spoiler</summary>

``` python
timbitsLeft = int(input()) 
totalCost = 0  


bigBoxes = timbitsLeft // 40              
totalCost = totalCost + bigBoxes * 6.19    # update the total price
timbitsLeft = timbitsLeft - 40 * bigBoxes  # calculate timbits still needed          


if timbitsLeft >= 20: # can we buy a medium?
  totalCost = totalCost + 3.39
  timbitsLeft = timbitsLeft - 20
if timbitsLeft >= 10: # can we buy a small?
  totalCost = totalCost + 1.99
  timbitsLeft = timbitsLeft - 10             # yo we only paid for 10, whys it giving 20? make that 10


totalCost = totalCost + timbitsLeft * 0.20   # why's our single timbits 20 dollars? make that 0.20
print(totalCost)
```
</details>


---
---

# 07: Rich Editor

Idk, some PSA to use a good IDE to make programming easier. I reco VSCode, or Neovim if you know vim-motion

---
---

# --- SESSION END ---
**Post-session Discussion:**

Yeah, 06 is long asf man, I ain't doing triplets today, SIX-SEV--- I'll skin you alive.

But yeah, the timbits one is pretty nice, debugging is definitely a very important skill in programming.

Signing off
