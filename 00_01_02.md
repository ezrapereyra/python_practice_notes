# 0: Hello!

- `print("")` : send message to output

*Example*
``` python
print("Hello, World!")

# Output: Hello World!
```

--- 

>*Coding Exercise: Bonjour*

 - Print the following phrase: Bonjour, tout le monde!

Answer:
``` python
print("Bonjour, tout le monde!")
```

---
---

# 01: Variables

- **variables** - storage locations for data
- `<<variable name>> = <<value to store>>`

*Example*
``` python
myLuckyNumber = 13
print(myLuckyNumber + 1)
print(myLuckyNumber)
myLuckyNumber = 5 + 2
print(myLuckyNumber)

# Output:
# 14
# 13
# 7
```

---

>*Short Answer Exercise: x Marks the Spot*

- What is the value of x?
``` python
x = 10
x = x + x
x = x - 5
```

Answer: ` x = 15`

---

**Common Errors:**
 - undefined variables causes crashes
 - assignment syntax; 4 = x is not valid

 ---

>*Coding Exercise: Heads, Shoulders, Knees and Toes*

- Given x amount of people, get the count of heads, shoulders, knees, and toes

Answer:
``` python
heads = people
shoulders = people * 2
knees = people * 2
toes = people * 10
```

--- 

>*Scramble Exercise: Speed Calculator*

- Rearrange the given lines to correct the program:
``` python
totalDistance = uphillDistance + downhillDistance
print(averageSpeed)
averageSpeed = totalDistance / totalTime
totalTime = uphillTime + downhillTime
```

Answer:
``` python
totalDistance = uphillDistance + downhillDistance
totalTime = uphillTime + downhillTime
averageSpeed = totalDistance / totalTime
print(averageSpeed)
```

--- 

>*Coding Exercise: Exchange Program*

- Write a program to swap the values of two variables. `x` and `y` are given

Answer:
```python
temp = x
x = y
y = temp
```

---
---

# 01E: Errors

- **Syntax Errors** - Python does not understand your syntax
```python
print "Gee Golly"
print(Hello, World!)
class = "Advanced Computronics for Beginners" # class is a special keyword!
name = "Jim
print(77
```
*In english: Please cat the dog*
- **Run-Time Errors** - Python gets it but runs into trouble while running
``` python
callMe = "Maybe"
print(callme) # case sensitive, callme is not defined
print(1/0) # trynna kill your pc?
print("you cannot add text and numbers" + 12) # aint meant to be
```
*In english: Please eat the piano*

- **Note**: Run-Time errors still run but will crash. Unlike Syntax Errors which will not run at all.

- **Logic Errors** - The program will run without crashing but will throw unexpected outputs
``` python
# you may want to calculate the average of two numbers and wrote:
x, y = 3, 4
average = x + y / 2
print(average)
# Output = 5

# idiot, PEMDAS
```

- **Note**: Logic Errors are the hardest to find and fix as programs only complain about your grammar and instructions but will happily jump off the cliff if it was told to and if it can.

---
---

# 02: Functions
- To use, call the function name and provide arguments if needed: `functionName(args)`
- In this lesson, we will introduce pre-defined functions
``` python
print("Hello World")
# print is the function name, "Hello World" is a string literal and is an argument

max(1, 2)
# max is a function to fin the highest input number, this one will return 2

min(1, 2)
# min does the opposite, 1

a, b = 3, 2

max(a, b)
min(a, b)
# variables are the usual use for arguments
```

---

>*Multiple Choice Exercise: Min and Max I*
- What is the output of the program?

``` python
x = 13
y = 7
a = max(x+y, x*2)
b = min(x, y)
print(a,b)
```

Answer: `26 7`

---

>*Short Answer Exercise: Min and Max II*
- What is the output of the following program?
``` python
x = min(max(13, 7), 9)
print(x)
```

Answer: 9

*Note: Introduction how to use pre-defined functions. You will later learn how to make one.*

---

**Common Errors:**

- If you call a function with not enough or too many arguments, it will throw an error
``` python
max() # too few arguments
```

---

>*Coding Exercise: One Road*
- Write a program that prints out the maximum weight that can be transported along this road. Your code should assume that the variables a, b, and c already contain the bridge weight limits.

Answer:
``` python
print(min(a,b,c))
```

---

>*Coding Exercise: Two Roads*
- Your truck can take either route. Write a program that prints out the maximum weight that can be transported between the two cities. Assume that the variables a, b, c, d, and e contain the bridge weight limits.

Answer:
``` python
print(max(min(a,b,c),min(d,e)))
```

---

>*Scramble Exercise: Sort of Strangeness*
- Arrange the code so that it prints the three numbers x, y and z sorted in increasing order, so that the smallest is printed first, then the middle one, and then the largest one.
``` python
print(max(x, y, z))
print(min(x, y, z))
print(x+y+z-min(x, y, z)-max(x, y, z))
```

Answer:
``` python
print(min(x, y, z))
print(x+y+z-min(x, y, z)-max(x, y, z))
print(max(x, y, z))
```

---
---

# 02X: Extra Practice
- This lesson contains extra practice exercises, some of which are challenging.

---

>*Coding Exercise: Growth Debugging*
- Fix the logic error in this program: it should calculate the population of your country for the next 3 years, assuming it starts with 1000 people in 2012, and the number of people increases by 10% each year. You can change at most three characters.
``` python
populationIn2012 = 1000
populationIn2013 = populationIn2012 * 0.1
populationIn2014 = populationIn2013 * 0.1
populationIn2015 = populationIn2014 * 0.1
```

Answer:
``` python
populationIn2012 = 1000
populationIn2013 = populationIn2012 * 1.1
populationIn2014 = populationIn2013 * 1.1
populationIn2015 = populationIn2014 * 1.1
```

---

>*Coding Exercise: Complication*
- Assume that the grader defines two variables A and B for you. Write a program which prints out the value. However, there is a catch: your program is not allowed to use the min function. Instead, use max in a clever way to simulate min. 
``` python
min(A, B)
```

Answer:
``` python
print(A + B - max(A, B))
```

---

>*Coding Exercise: Payment Calculator*
- A credit card company computes a customer's "minimum payment" according to the following rule. The minimum payment is equal to either $10 or 2.1% of the customer's balance, whichever is greater; but if this exceeds the balance, then the minimum payment is the balance. Write a program to print out the minimum payment using min and max. Assume that the variable balance contains the customer's balance. Your program does not need to print the dollar sign.

*Example:*  
Example 1: if your balance is 1000, then your program should print 21.  
Example 2: if your balance is 600, then your program should print 12.6.  
Example 3: if your balance is 25, then your program should print 10.  
Example 4: if your balance is 8, then your program should print 8.  

Answer:
``` python
print(min(balance, max(10, balance * 0.021)))
```

--- 

>*Scramble Exercise:Sorting Scramble*
 -Code scramble: make the program sort the three numbers x, y and z into increasing order, so that x has the smallest value, y has the next smallest value, and z has the largest value.
``` python
y = tmp
y = min(y, z)
x = min(x, y)
tmp = max(y, z)
tmp = max(x, y)
x = min(x, y)
y = tmp
z = tmp
tmp = max(x, y)
```

Answer:
``` python
# This is a headache
tmp = max(x, y)
x = min(x, y)
y = tmp
tmp = max(y, z)
y = min(y, z)
z = tmp
tmp = max(x, y)
x = min(x, y)
y = tmp
```

---
---

# --- SESSION END ---
### Post-session Discussion:
Today is pretty chill, the 3 lessons are basic and is no prblem. 2X extra practice however was pretty challenging, it actaully lived up to its promise to be hard. Anyways, it's mostly just a recall on how to use variables pretty much.

Signing Off